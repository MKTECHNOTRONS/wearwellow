'use strict';
var jq, BASE_URL = "/apps/otp",
    PATH_WIDGET = BASE_URL + "/widget/edd",
    SHOP = window.Shopify.shop;

function loadJs(a, c) {
    var b = document.createElement("script");
    b.src = a;
    a = document.getElementsByTagName("head")[0];
    b.onload = function() {
        c()
    };
    a.appendChild(b)
}

function run() {
    console.log("Starting OTP Widget");
    jq.ajax({
        type: "GET",
        url: PATH_WIDGET
    }).done(function(a) {
        null != a && jq("#gz-otp-eddw").html(a)
    })
}

function getUrlParam(a) {
    var c = window.location.search.substring(1).split("&"),
        b;
    for (b = 0; b < c.length; b++) {
        var d = c[b].split("=");
        if (d[0] === a) return decodeURIComponent(d[1])
    }
    return !1
}
loadJs("//code.jquery.com/jquery-3.5.1.min.js", function() {
    jq = $.noConflict(!0);
    run()
});